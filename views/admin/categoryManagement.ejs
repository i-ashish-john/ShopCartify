<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Category List</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    main {
      padding: 20px;
    }
  
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
  
    th, td {
      padding: 12px 15px;
      text-align: left;
    }
  
    th {
      background-color: rgb(224, 121, 121);
      color: #fff;
      border-radius: 10px;
    }
  
    tr:nth-child(even) {
      background-color: rgb(255, 255, 255);
    }
  
    tr:hover {
      background-color: #f5c0c0;
    }
  
    td a {
      display: inline-block;
      text-decoration: none;
      padding: 5px 10px;
      background-color: #feffff;
      color: #fff;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
  
    td a:hover {
      background-color: #0056b3;
    }
  
    .btn-block {
      width: 50px;
      padding:0px;
    }
  
    @media (max-width: 768px) {
      nav {
        width: 100%;
        order: -1;
      }
  
      .container {
        padding: 0;
      }
      .add-button {
        margin-left: 0; 
        width: 100%; 
      }
    }
    
  </style>
</head>

<body>
  <header>
    <h1 style="margin-left: 200px;">Category Management</h1>
  </header>

  <div class="container flex">
    <nav>
      <ul class="nav flex-column">
        <!-- Your navigation links go here -->
      </ul>
    </nav>

    <main>
      <!-- <h2 style="color:rgb(135, 78, 128)">USER MANAGEMENT</h2> -->
      <!-- <nav>
        <button id="dashboardBtn" class="btn btn-primary" onclick="redirectToDashboard()">&#8592; Dashboard</button>
        <ul class="nav flex-column">
           Your navigation links go here -->
        </ul>
      </nav> 
      <!-- <div class="search-container">
        <form action="/admin/usermanagement" method="Get">
          <input type="text" name="search" id="search-input" class="search-input" placeholder="Search...">
          <button type="submit" class="search-button">Search</button>
        </form>
      </div> -->

      <!-- Uncomment the following section if needed -->
      <!-- <h3>Search Results for: <%= locals.searchQuery %></h3> -->
      <div class="text-right">
        <button class="add-button btn-danger" onclick="redirectToCategoryAdd()">Add Category</button>
      </div>
      <table class="table" style="margin-left:70px;"> 
        <thead>
            <tr>
                <th>Category Name</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
            
        </thead>
        <tbody>
            <% categories.forEach(category => { %>
                <tr>
                    <td><%= category.categoryName %></td>
                    <td><%= category.categoryDescription %></td>
                    <td>
                      <form method="post" action="/admin/categoryManagementdelete/<%= category._id %>">
                        <button type="submit" class="btn btn-success ">DELETE</button>
                      </form><br>
                      <form method="post" action="/admin/categoryeditpage/<%= category._id %>">
                        <button type="submit" class="btn btn-warning ">EDIT</button>
                      </form>

                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
  
    </main>
    <%-include("../partials/adminnavigation.ejs")-%>

  </div>

  <!-- Bootstrap JS and Popper.js (for Bootstrap) -->
  <!-- <script>
    function redirectToDashboard() {
      // Add your instructions or redirect code here
      window.location.href = '/admin/dashboard'; // Redirect to the dashboard URL
    }
  </script> -->
  <script>
    function redirectToCategoryAdd() {
      window.location.href = '/admin/categoryadd'; 
    }
  </script>
   <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
   <script>
       document.addEventListener('DOMContentLoaded', function () {
         // This ensures that the DOM is fully loaded before executing the script
   
         // Your Swal.fire code here
         Swal.fire({
           title: 'Warning!',
           text: 'Are you sure about changing detils in category',
           icon: 'warning',
           showCancelButton: true,
           confirmButtonText: 'Yes',
           cancelButtonText: 'No'
         }).then((result) => {
        // If the user clicks "Yes"
        if (result.isConfirmed) {
          // Your logic for "Yes" goes here
          console.log('User clicked "Yes"');
        } else if (result.dismiss === Swal.DismissReason.cancel) {
          // Your logic for "No" (Cancel) goes here
          res.redirect('/admin/dashboard');
          console.log('User clicked "No" or closed the dialog');
        }
      });
       });
     </script>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd" ></script>
  <!-- Bootstrap JS and Popper.js (for Bootstrap) -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
