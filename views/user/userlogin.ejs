<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ShopCartify</title>
  <style>
      body {
          margin: 0;
          padding: 0;
          font-family: 'Open Sans', sans-serif;
          background: url('/path/to/your/background-image.jpg') no-repeat center center fixed;
          background-size: cover;
          height: 100vh;
          display: flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          /* Prevent background overflow */
      }

      .overlay {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background: rgba(0, 0, 0, 0.5);
          z-index: 1;
          display: flex;
          align-items: center;
          justify-content: center;
          opacity: 0;
          pointer-events: none;
          transition: opacity 0.3s ease-in-out;
          backdrop-filter: blur(10px);
          /* Background blur effect */
      }

      .login-form {
          background: rgba(255, 255, 255, 0.8);
          padding: 20px;
          border-radius: 10px;
          width: 300px;
          text-align: center;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
          /* Box shadow for depth */
      }

      .login-form input {
          width: 100%;
          padding: 10px;
          margin-bottom: 10px;
          border: 1px solid #ccc;
          border-radius: 5px;
          transition: border-color 0.3s ease-in-out;
      }

      .login-form input[type="submit"] {
          background-color: #4CAF50;
          color: white;
          cursor: pointer;
      }

      .login-form input:focus {
          border-color: #bd7825;
          /* Highlight input on focus */
      }

      .login-form p {
          margin: 10px 0;
          color: #bd7825;
          /* Custom color for messages */
      }

      .create-account-button {
          background-color: #BC506F;
          color: beige;
          border: none;
          border-radius: 18px;
          padding: 10px 20px;
          text-decoration: none;
          cursor: pointer;
      }
  </style>
</head>

<body>
  <div class="overlay" id="loginOverlay">
      <div class="login-form">
          <h2 style="color: #bd7825;">Login Now</h2>
          <form id="loginForm" action="/login" method="post">
              <input type="email" name="email" placeholder="Email@example.com" value="johnashish509@gmail.com"
                  required>
              <br>
              <input type="password" name="password" placeholder="Password" value="12345678" required>
              <br>
              <label class="checkbox">
                  <input type="checkbox" name="checkbox" checked="">
                  <i></i> Keep me logged in
              </label>
              <br>
              <input type="submit" value="Log in">
              <p style="color: red;">
                  <%=locals.validation%>
              </p>
              <p style="color: bisque;">Don't have an account?</p>
              <a href="/signup" class="create-account-button">Create new</a>
          </form>
      </div>
  </div>

  <script>
      // Show login overlay
      document.addEventListener('DOMContentLoaded', function () {
          const overlay = document.getElementById('loginOverlay');
          overlay.style.opacity = 1;
          overlay.style.pointerEvents = 'auto';
      });
  </script>
</body>

</html>